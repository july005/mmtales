<!--<table>
	<%# @posts.in_groups_of(3, false) do |row_post| %>
  <tr>
    <%# for post in row_post %>
      <td><%#= link_to post.title, post_path(post) %><%#= image_tag post.image.url(:small), :class => "img-responsive" %></td>
    <%# end %>
  </tr>
<%# end %>
</table>-->

<div id="posts" class="transitions-enabled"> 
  <% @posts.each do |pin| %>
    <div class="box panel panel-default">
    	<%= link_to image_tag(post.image.url(:small)), post%>
      <div class="panel-body">
        <p><%= post.title %></p>
      </div>
    </div>
  <% end %>
</div>

