<%= form_for [@post], html: { multipart: true } do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :subtitle %><br>
    <%= f.text_field :subtitle %>
  </div>
  <div class="field">
    <%= f.label :tags %><br>
    <%= f.text_field :tag_list %>
  </div>
  </br>
    <%= f.label :body %>
    <%= f.text_area :body, id: "wysiwyg" %> 
  
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>

<script>

 $('#wysiwyg').froalaEditor({
    inlineMode: false,
    heightMin: '200px',
    imageUploadToS3: {
      bucket: '<%= @hash[:bucket] %>',
      region: 's3', // Change the region if it is different
      keyStart: '<%= @hash[:key_start] %>',
      callback: function (url, key) {
        console.log (url);
        console.log (key);
      },
      params: {
        acl: '<%= @hash[:acl] %>',
        AWSAccessKeyId: '<%= @hash[:access_key] %>',
        policy: '<%= @hash[:policy] %>',
        signature: '<%= @hash[:signature] %>',
      }
    },
    colorsText: ['#76c4ae', '#61BD6D', '#1ABC9C', '#54ACD2', '#2C82C9', '#9365B8', '#475577', '#CCCCCC', '#41A85F', '#00A885', '#3D8EB9', '#2969B0', '#553982', '#28324E', '#000000', '#F7DA64', '#FBA026', '#EB6B56', '#E25041', '#A38F84', '#EFEFEF', '#FFFFFF', '#FAC51C', '#F37934', '#D14841', '#B8312F', '#7C706B', '#D1D5D8', 'REMOVE'],
    fontFamily: {
        "Roboto,sans-serif": 'para - Roboto',
        "'Vibur',sans-serif": 'head - Vibur',
        "'Special Elite',sans-serif": 'Question - SpecialElite',
      },
      fontFamilySelection: true,
      fontSizeDefaultSelection: '18',
      fontSize: ['8', '10', '12', '13', '14', '15', '16', '17', '18', '19', '20', '30', '60', '96'],
    paragraphStyles: {
        froalapara: 'Para',
        froalahead: 'Heading',
        froalaquestion: 'Question',
      },
    fileUploadToS3: {
      bucket: '<%= @hash[:bucket] %>',
      region: 's3', // Change the region if it is different
      keyStart: '<%= @hash[:key_start] %>',
      callback: function (url, key) {
        console.log (url);
        console.log (key);
      },
      params: {
        acl: '<%= @hash[:acl] %>',
        AWSAccessKeyId: '<%= @hash[:access_key] %>',
        policy: '<%= @hash[:policy] %>',
        signature: '<%= @hash[:signature] %>',
      }
    }
 })
</script>


      

